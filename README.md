# Fun C++ examples

A repository with fun and experimental C++ examples. 

# Run command

1. From `app` or `game` project folder, run below to build the game
```
cmake -S . -B build
cmake --build build
```

2. To show out on the console, run 

```
./build/<Game-name>
```
